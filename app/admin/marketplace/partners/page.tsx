import { requireMarketplaceAdministrator } from "../../../marketplace-authorization";
import { syntheticPartnerOpportunities } from "../../../growth/fixtures";
export const dynamic="force-dynamic"; export const metadata={title:"Partner Hunt",robots:{index:false,follow:false}};
const states=["High-value referral","Recurring revenue","Merchant residual","Manufacturer direct","Software","Services","Financing","Growth / advertising","Needs research","Needs application","Needs outreach","Negotiation opportunity","Verified","Rejected"];
export default async function PartnerHuntPage(){await requireMarketplaceAdministrator("/admin/marketplace/partners");return <main className="partner-hunt"><header><p>Founder-only · synthetic presentation fixture</p><h1>Partner Hunt</h1><p>Find strong customer economics first. Research the commercial relationship second.</p></header><section aria-label="Partner opportunity states" className="partner-state-grid">{states.map(state=><article key={state}><h2>{state}</h2><span>No verified opportunities yet</span></article>)}</section><section><h2>Synthetic examples</h2>{syntheticPartnerOpportunities.map(item=><details key={item.id}><summary>{item.programName}</summary><p>{item.providerName} · {item.commercialLane} · {item.relationshipStatus}</p><p>Economics: unknown. Evidence: insufficient. Not a real partner claim.</p></details>)}</section></main>}
