import {isAllowedEarlyAccessEndpoint} from "./loopsAdapter.ts";
export type EmailCaptureHealth={status:"READY"|"MISCONFIGURED"|"NOT CONFIGURED";newsletter:boolean;earlyAccess:boolean;provider:"Loops-compatible adapter";detail:string};
export function getEmailCaptureHealth(environment:NodeJS.ProcessEnv=process.env):EmailCaptureHealth{const early=environment.EARLY_ACCESS_ENDPOINT||environment.EMAIL_SUBSCRIBE_ENDPOINT;const newsletter=environment.EMAIL_SUBSCRIBE_ENDPOINT||environment.EARLY_ACCESS_ENDPOINT;if(!early&&!newsletter)return {status:"NOT CONFIGURED",newsletter:false,earlyAccess:false,provider:"Loops-compatible adapter",detail:"No email provider endpoint is configured."};const earlyAccess=Boolean(early&&isAllowedEarlyAccessEndpoint(early));const newsletterReady=Boolean(newsletter&&isAllowedEarlyAccessEndpoint(newsletter));return {status:earlyAccess&&newsletterReady?"READY":"MISCONFIGURED",newsletter:newsletterReady,earlyAccess,provider:"Loops-compatible adapter",detail:earlyAccess&&newsletterReady?"Both capture routes have an allowed provider endpoint.":"One or more capture routes have a missing or disallowed endpoint."};}
